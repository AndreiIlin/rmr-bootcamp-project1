import React, { FC } from 'react';
import UserReportsContainer from '../../features/reports/components';

const UserReportsPage: FC = () => {
  return <UserReportsContainer />;
};

export default UserReportsPage;
